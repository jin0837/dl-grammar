# 2026-01-04

## 今日やったこと
- Softmax 実装の数値安定化について復習
- `exp_x = np.exp(x - np.max(x))` の意味を確認
- セグメンテーション実験ログを読み、クラス10が消失する問題を確認
- Warmup 導入後に偽陽性の揺れが減少したことを確認

## 学んだこと

### 数値安定化の意味
Softmax の実装における

exp_x = np.exp(x - np.max(x))


は、指数計算時のオーバーフロー防止のための処理。


